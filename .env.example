# Proxy endpoint — points to our own API route
NEXT_PUBLIC_INDEXER_URL=http://localhost:3000/api/graphql

# Actual Hasura endpoint (server-side only)
INDEXER_URL=https://your-hasura-endpoint.example.com/v1/graphql